# Memory Index

## Project
- [project] Always create a new dedicated branch for major code changes → project-conventions.md
- [project] AG Kit only supports Gemini CLI and Google Antigravity (not other AI coding tools) → project-conventions.md
- [project] Never use `late final` in Riverpod Notifier build() methods (causes LateInitializationError on refresh) → project-conventions.md
- [project] Riverpod Stream + Controller Pattern: Use StreamProvider.family for real-time reads alongside an imperative Controller Provider for mutations to avoid FamilyNotifier type binding pitfalls → project-conventions.md
- [project] Component metadata uses SemVer while toolkit releases use CalVer → tech-decisions.md
- [project] Use global AppSnackbar utility for UI notifications → tech-decisions.md
- [project] Exception-Driven Auth Routing with persistent session during email verification → tech-decisions.md
- [project] Applicant persistent 4-tab bottom navigation using GoRouter StatefulShellRoute → tech-decisions.md
- [project] Admin persistent 4-tab bottom navigation using GoRouter StatefulShellRoute (Dashboard, Committees, Add Member, Settings) → tech-decisions.md
- [project] Reactive appointment tab selection and fallback via selectedAppointmentIdProvider → tech-decisions.md
- [project] Applicant profile photo upload to Storage and Firestore photoUrl integration → tech-decisions.md
- [project] Production Firestore and Storage security rules with RBAC and size limits → tech-decisions.md
- [project] Profile Photo Pipeline: Upload & Remove lifecycle with ImageCropper (1:1 square), client-side compression (1024px max, 70% quality JPG), SafeProfileAvatar with App Check headers and fallback icon, global Riverpod upload loading state (`profileViewModelProvider.isLoading`), Material-backed options bottom sheet (`_showPhotoOptionsSheet`), old photo Storage cleanup on update & delete (`deleteFileByUrl`), Firestore `FieldValue.delete()` on photo removal, and Firestore rules `photoUrl` regex enforcement (`matches('^https://.*')`). → tech-decisions.md
- [project] NEVER use static color constants (e.g., AppColors) or hardcoded Colors (e.g., Colors.grey) directly in UI code. ALWAYS use Theme.of(context).colorScheme (or AppStatusColors for semantic status badges) to support dark mode seamlessly. → ui-conventions.md
- [project] Theme Engine uses FlexColorScheme v8.4.0 with FlexScheme.greenM3, GoogleFonts Inter, FlexSubThemesData, and ResponsiveTheme wrapper in AppTheme. Persisted via Riverpod themeProvider and HiveStorageService. → tech-decisions.md
- [project] ALL Hive local storage access MUST go through HiveStorageService (lib/core/services/hive_storage_service.dart). NEVER call Hive.openBox() or Hive.box() directly in feature code. Use typed accessors: getThemeMode/setThemeMode, getWizardDraft/saveWizardDraft/clearWizardDraft, cacheData/getCachedData. Use clearUserData() on logout. → tech-decisions.md
- [project] For Flutter 3.32+, wrap radio lists in RadioGroup<T>(groupValue: ..., onChanged: ...) rather than placing groupValue/onChanged on individual RadioListTile/Radio widgets (which are deprecated). → ui-conventions.md
- [project] Buttons in Layouts: Always wrap buttons in Row with Expanded or set minimumSize: Size.zero. Avoid unconstrained buttons in ListTile.trailing due to Size.fromHeight(50) theme rules → ui-conventions.md
- [project] Modal Action Sheets: Use showModalBottomSheet with isScrollControlled: true, useSafeArea: true, and rounded top corners (24.r) for administrative workflows (Assign Reviewer, Assign Task, Set Confirmed Date). → ui-conventions.md
- [project] Booking Wizard Draft Persistence: Synchronous Hive loading in BookingWizardViewModel.build(), surviving app process death, with Home Screen Resume Survey (Step X) and Start Fresh flows → tech-decisions.md
- [project] Password Reset Hardening: Email regex pre-validation, awaited execution with try/catch, 60s cooldown timer, and loading spinner → tech-decisions.md
- [project] Proactive Session Lifecycle & Deactivation Guard: userChanges() stream + AppLifecycleListener(onResume) calling user.reload() and checking Firestore active == false to boot revoked/deactivated sessions immediately with a global warning snackbar. Crashlytics user_id and role custom keys bound on auth. → tech-decisions.md
- [project] Logout Full Isolation (Option 3): clearUserData() wipes Hive cache and wizard drafts; BookingWizardViewModel resets in-memory state on auth change → tech-decisions.md
- [project] Admin Cloud Function Mutations: Assign Reviewer, Assign Task, and Set Confirmed Date execute via Cloud Functions/repositories to enforce admin RBAC and keep Firestore secure. → tech-decisions.md
- [project] File Picker v12 & Optional Multi-Document Upload: file_picker v12.0.0 federated architecture (pickFile for single, pickFiles for batch, await file.length() for size). Step 7 permission documents are optional (min 0, max 5, <= 5MB each, PDF/JPG/PNG) with client-side deduplication and slot capping. → tech-decisions.md
- [project] Transparent Upload Progress & Storage MIME Metadata: StorageUploadService and BookingWizardViewModel report bytes via onProgress; Step8Review renders animated live progress card on submission. SettableMetadata(contentType: ...) attached to all Storage uploads. → tech-decisions.md
- [project] StatefulShellRoute Branch Back Button Interception: Wrap all non-home branch routes in PopScope(canPop: false, ...) to route to Home/Dashboard (index 0) on back button press before allowing exit on Home. → tech-decisions.md
- [project] Two-Phase Role-Aware Routing & Zero-Flash Splash Lifecycle: AsyncAuthNotifier notifies GoRouter first to redirect, then removes native splash via WidgetsBinding.instance.addPostFrameCallback after the frame renders; initialLocation set to /login to eliminate UI flicker → tech-decisions.md
- [project] Riverpod Notifier build() Stream Listening: Never use fireImmediately: true in ref.listen inside build() when callback accesses state. Use synchronous ref.read for initial state. → tech-decisions.md
- [project] Committee Member Provisioning & Credentials Presentation: `createCommitteeAccount` 2nd Gen callable Cloud Function in `functions/src/index.ts` validates admin role, provisions Auth user (`emailVerified: true`), mints `role: 'committee'`, writes Firestore `/users/{uid}` with phone number, and returns credentials map (`tempPassword`, `email`, `resetLink`). `AddCommitteeMemberScreen` presents non-dismissible `_showCredentialsDialog` with `SelectableText` and one-touch Copy action before form reset and navigation. → tech-decisions.md
- [project] Admin Functions Execution & Cloud Run 2nd Gen IAM: AdminFunctionsService wrapper performs pre-flight claim checks & token refreshes; createCommitteeAccount uses invoker: 'public', cors: true, enforceAppCheck: false, and requires Compute Engine service account to have Firebase Admin role → tech-decisions.md
- [project] Server-Mediated Auth Rate Limiting (Option A): Login/Signup/PasswordReset go through Cloud Functions (NOT direct Firebase Auth client SDK). `authenticateUser` CF verifies credentials via Identity Toolkit REST API, enforces failure-based rate limits (5 failures → 15-min lock for applicants; 3 failures → 30-min lock for admin), and returns a Firebase Custom Token. Flutter calls `signInWithCustomToken()`. `registerApplicant` CF creates accounts via Admin SDK with count-based limit (3/hour per email). `requestPasswordReset` CF sends Firebase-branded reset email via Identity Toolkit REST API with 3/hour limit. Rate limit state lives in Firestore `auth_rate_limits/` collection (locked to Admin SDK: `allow read, write: if false`). `AUTH_WEB_API_KEY` secret required for `authenticateUser` and `requestPasswordReset` functions. → tech-decisions.md
- [project] AuthFunctionsService (`lib/core/services/auth_functions_service.dart`): Unauthenticated CF caller for login/signup/reset. Unlike AdminFunctionsService, no pre-flight auth check. Translates `resource-exhausted` → `AuthRateLimitFailure(message, secondsRemaining)`. Login screens show a live `_loginLockoutSeconds` countdown on the Login button when `AuthRateLimitFailure` is received. → tech-decisions.md
- [project] Firebase Local Emulator Suite & 112-Assertion Testing Suite: `FirebaseEmulator` (`lib/core/firebase/firebase_emulator.dart`) uses `const bool kUseFirebaseEmulator = bool.fromEnvironment('USE_FIREBASE_EMULATOR', defaultValue: false);` to conditionally connect Auth (:9099), Firestore (:8080), Storage (:9199), and Functions (:5001) with Android `10.0.2.2` routing, `persistenceEnabled: false`, and conditional App Check bypass. Automated Node.js testing harness under `firebase/tests/` (@firebase/rules-unit-testing + firebase-admin + Jest) verifies 112 assertions across 8 suites: 88 Security Rules assertions (`users`, `appointments`, `audit_log`, `rate_limits`, `appointment_files`, `profile_photo`) + 24 Cloud Functions assertions (`authz.test.js` RBAC guards, `race_condition.test.js` atomic bursts). Test helpers (`test_helpers.js`) use Auth emulator REST endpoint (`accounts:signInWithCustomToken`) for authentic ID tokens and direct REST DELETE for database flushes. Local secret mock in `functions/.secret.local`. Complete user manual at `docs/firebase_emulator_user_manual.md`. → tech-decisions.md
- [project] Centralized Appointment Status Badges and Booking Cards: Replaced duplicated `_buildStatusBadge` and `_buildBookingCard`/`_buildAppointmentCard` implementations across home, bookings, and detail screens with shared `AppointmentStatusBadge` and `AppointmentBookingCard` widgets in `lib/core/widgets/`. → ui-conventions.md
- [project] Riverpod State Decoupling for Form Focus: Use Riverpod state management rather than local `setState` for complex form replies (such as clarification replies in `AppointmentDetailScreen`) to prevent keyboard auto-closing and screen re-render flashes on textfield tap. → tech-decisions.md
- [project] Cross-Feature Reactive Cache Invalidation: When mutators update appointment lifecycle states in secondary screens (e.g. `MyBookingsViewModel.submitClarificationReply`), call `ref.invalidate(homeViewModelProvider)` to ensure one-shot dashboard caches immediately refresh with current status badges. → tech-decisions.md
- [project] Server-Mediated Appointment Mutations & Review UX: Direct Firestore writes to `/appointments/{id}` are blocked by rules (`allow update: if false;`), causing optimistic-revert button flicker. All mutations (approve, reject, clarify, assignments, date) route through 2nd Gen Cloud Functions (`reviewAppointment`, etc.). Includes confirmation modal, inline spinners preserving button geometry to eliminate layout shifts, atomic quota decrements, and dashboard cache invalidation (`ref.invalidate(committeeDashboardStreamProvider)`). → tech-decisions.md
- [project] Firestore Identity-Based Appointment Reads & Custom Token Claims Synchronization: Appointment reads use `(isAuthenticated() && resource.data.applicantId == request.auth.uid)` to prevent `PERMISSION_DENIED` on cached/legacy sessions lacking custom claims in ID token. Helper functions safely guard `'role' in request.auth.token`. `authenticateUser` bakes `{role: userRole}` into `createCustomToken` and syncs custom claims via Admin SDK. → tech-decisions.md
- [project] Committee Review Execution & Firestore Read-Before-Write Order: In `reviewAppointment` Cloud Function, all reads (`appointmentRef`, `rateLimits`) must execute in Phase 1 before any writes (`appointmentRef` update, `rateLimits` quota decrement, `auditLog` entry) in Phase 2 to satisfy Firestore transaction invariants. Client repository uses proactive token refresh (`_refreshToken()`), App Check debug token registration, and Cloud Run public invoker permissions. → tech-decisions.md
- [project] Resilient Admin Cloud Function Guards & Token Claims Backfilling: In callable Cloud Functions (`admin_appointment_mutations.ts`), use an async `requireAdmin` guard that checks deactivation via Firestore, verifies ID token custom claims, falls back to the `/users/{uid}` Firestore document role when custom claims are stale, and backfills `auth.setCustomUserClaims(uid, {role: 'admin'})`. On Flutter, always call through `AdminFunctionsService` with `getIdToken(true)` and `on Failure { rethrow; }`. → tech-decisions.md
- [project] Modal Action Sheet Pop-Before-Snackbar & PopScope Guard: In modal bottom sheets (`AssignTaskSheet`, `AssignReviewerSheet`, `SetConfirmedDateSheet`), always dismiss the sheet (`context.pop()`) before displaying success or error notifications via `AppSnackbar.showGlobal*()`. This prevents snackbars from being hidden behind modal barriers or lost in unmounted modal route contexts. Wrap with `PopScope(canPop: !_isSubmitting)` to disable back gestures while mutations run. → ui-conventions.md
- [project] Bottom Sheet Material Canvas & Flutter 3.32+ RadioGroup Hierarchy: In modal sheets where `Container(decoration: ...)` is used, wrap list contents in `Material(color: Colors.transparent)` to satisfy the framework requirement and prevent the `ListTile background color or ink splashes may be invisible` assertion. Wrap in `RadioGroup<T>` ancestor rather than passing deprecated `groupValue`/`onChanged` to individual `RadioListTile` widgets. → ui-conventions.md
- [project] Full-Stack FCM Push Notifications & DoS Array Security Bounds: End-to-end push architecture across 7 domain types. `FirebaseNotificationService` is the sole `FirebaseMessaging` caller. In-app foreground alerts rendered via `NotificationOverlayWrapper`/`NotificationOverlay`; background/terminated taps handled via `NotificationHandler` with auth-gated deep linking via `NotificationNavigation`. Backend multicasts via Cloud Functions with automatic stale token pruning (`pruneInvalidToken`). `firestore.rules` enforces `fcmTokens is list && fcmTokens.size() <= 10` on `/users/{uid}` to protect against document bloat and DoS. → tech-decisions.md
- [project] Storage Rules 2-Read Cap & Pre-Generated Appointment ID Synchronization: Cloud Storage rules enforce a maximum of 2 Firestore reads per evaluation. `isAssignedCommittee()` short-circuits on `isCommittee()` first (0 reads for applicants). Client pre-generates appointment ID (`appointmentRepo.newAppointmentId()`) before uploading KML/docs so Storage paths (`appointments/{id}/...`) match Firestore (`/appointments/{id}`). `SettableMetadata` tags `uploadedBy: uid` for 0-read auth; `isUnlinkedAppointmentFile` fallback covers legacy/orphan files. → tech-decisions.md
- [project] Riverpod Pull-to-Refresh & Scrollable Child Architecture: Never assign state = const AsyncLoading() or call build() in refresh(); assign state = await AsyncValue.guard(...) directly to preserve UI during pull-to-refresh without dual spinners or screen collapse. Await ref.watch(authViewModelProvider.future) in build() to eliminate startup false-empty flicker. Always ensure RefreshIndicator wraps a scrollable widget (ListView or SingleChildScrollView with AlwaysScrollableScrollPhysics) in all states. In tab views, check state.hasValue to avoid tearing down active views during background reloads. Implement operator == and hashCode on models like AppUser. → tech-decisions.md


